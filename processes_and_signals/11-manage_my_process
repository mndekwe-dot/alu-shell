#!/usr/bin/env bash
# This script continuously writes "I am alive!" to a log file every 2 seconds.

LOG_FILE="/tmp/my_process"

# Infinite loop
while true; do
    echo "I am alive!" >> "$LOG_FILE"
    sleep 2
done
