#!/usr/bin/env bash
# This script displays the content of the current directory in a list format,
# showing only the part of the filename after the first dash

for file in *; do
  # Skip hidden files (those starting with .)
  if [[ "$file" != .* ]]; then
    echo "$file" | cut -d'-' -f2-
  fi
done

